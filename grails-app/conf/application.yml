---
grails:
    profile: web-plugin
    codegen:
        defaultPackage: com.budjb.hazelcast.session
info:
    app:
        name: '@info.app.name@'
        version: '@info.app.version@'
        grailsVersion: '@info.app.grailsVersion@'
